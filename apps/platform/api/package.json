{
  "name": "@codexsun/platform-api",
  "version": "1.0.71",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node ../../../tools/preflight.mjs platform-api",
    "dev:skip-db": "node ../../../tools/preflight.mjs platform-api --skip-db",
    "dev:fast": "node ../../../tools/preflight.mjs platform-api --skip-db",
    "build": "tsc -p tsconfig.json",
    "db:migrate": "tsx src/db/cli.ts migrate",
    "db:migrate-fresh": "tsx src/db/cli.ts migrate:fresh --confirm",
    "db:migrate-rollback": "tsx src/db/cli.ts migrate:rollback --confirm",
    "db:tenant:migrate": "tsx src/db/cli.ts migrate --scope=tenant --all-tenants",
    "db:tenant:migrate-fresh": "tsx src/db/cli.ts migrate:fresh --scope=tenant --all-tenants --confirm",
    "db:tenant:migrate-rollback": "tsx src/db/cli.ts migrate:rollback --scope=tenant --all-tenants --confirm",
    "db:tenant:drop": "tsx src/db/cli.ts drop --scope=tenant --all-tenants --confirm",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@codexsun/billing": "^1.0.71",
    "@codexsun/core": "^1.0.9",
    "@codexsun/framework": "^1.0.9",
    "@codexsun/platform": "^1.0.9",
    "@fastify/cookie": "^11.0.1",
    "dotenv": "^17.4.2",
    "fastify": "^5.2.1",
    "mysql2": "^3.11.5",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
